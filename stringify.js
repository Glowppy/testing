const luogo = {
    stato: "Italia",
    regione: "Puglia",
    città: "Taranto",
    quartiere: "Talsano"
  };
  
  const miastringajson = JSON.stringify(luogo);
  
  console.log(miastringajson);

  // La funzione JSON.stringify prende una stringa e mostra i singoli elementi all'interno